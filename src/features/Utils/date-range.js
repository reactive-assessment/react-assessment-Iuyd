/**
 * Compute friendly name for a date-range
 *
 * @param {Date} fromDate
 * @param {Date} toDate
 *
 * @returns {String}
 */

const getText = (fromDate, toDate) => {
  return '';
};

export {getText};